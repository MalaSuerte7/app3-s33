 version: "3.3"

 services:
   api-students:
     build: ./api-students
     image: fcalleroot/api-students
     ports:
       - 8000:8000
   websimple:
     build: ./websimple
     image: fcalleroot/websimple
     ports:
       - 8080:80
